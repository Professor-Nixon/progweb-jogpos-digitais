function parImparr(num) {
    
    console.log(num % 2 == 0 ? `${num} é PAR` : `${num} é IMPAR`)
}

parImparr(10)

let pparImpar = function(num) {
    console.log(num % 2 == 0 ? "PAR" : "IMPAR")
}

pparImpar(10)

let parImpar = (num) => {
    console.log(num % 2 == 0 ? "PAR" : "IMPAR")
}

parImpar(15)